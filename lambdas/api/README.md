# Energy app Lambda functions

The Lambda functions here are copied to their corresponding Lambda through the AWS editing console.

The directory structure mirrors how things are set up on AWS API gateway